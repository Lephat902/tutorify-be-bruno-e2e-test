meta {
  name: Clear update
  type: http
  seq: 6
}

patch {
  url: {{base_url}}/tutors/profile
  body: json
  auth: inherit
}

body:json {
  {
    "address": null,
    "gender": null,
    "password": "{{sample_tutor_password}}",
    "oldPassword": "newpassword",
    "avatar": null,
    "socialProfiles": null,
    "tutorPortfolios": null
  }
}

assert {
  res.status: eq 200
  res.body.address: isNull
  res.body.gender: isNull
  res.body.avatar: isNull
}
